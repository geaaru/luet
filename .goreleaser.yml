#before:
#  hooks:
#    - go mod tidy
dist: release
source:
  enabled: true
  name_template: '{{ .ProjectName }}-{{ .Tag }}-source'
checksum:
  name_template: '{{ .ProjectName }}-{{ .Tag }}-checksums.txt'
builds:
  - id: anise
    binary: anise
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
      - -X 'github.com/macaroni-os/anise/pkg/config.BuildTime={{ time "2006-01-02 15:04:05 MST" }}'
      - -X 'github.com/macaroni-os/anise/pkg/config.BuildCommit={{ .FullCommit }}'
      - -X 'github.com/macaroni-os/anise/pkg/config.BuildGoVersion={{ .Env.GOVERSION }}'
    goos:
      - linux
    goarch:
      - amd64
      - arm
      - arm64
      - 386
    goarm:
      - 6
      - 7

  - id: anise-build
    dir: anise-build
    binary: anise-build
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
      - -X 'github.com/macaroni-os/anise/pkg/config.BuildTime={{ time "2006-01-02 15:04:05 MST" }}'
      - -X 'github.com/macaroni-os/anise/pkg/config.BuildCommit={{ .FullCommit }}'
      - -X 'github.com/macaroni-os/anise/pkg/config.BuildGoVersion={{ .Env.GOVERSION }}'
    goos:
      - linux
    goarch:
      - amd64
      - arm
      - arm64
      - 386
    goarm:
      - 6
      - 7


archives:
  - id: anise
    builds:
      - anise
    format: binary # this removes the tar of the archives, leaving the binaries alone
    name_template: >-
      anise-{{ .Tag }}-{{ .Os }}-
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{ if .Arm }}v{{ .Arm }}{{ end }}

  - id: anise-build
    builds:
      - anise-build
    format: binary
    name_template: >-
      anise-build-{{ .Tag }}-{{ .Os }}-
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{ if .Arm }}v{{ .Arm }}{{ end }}

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^Merge pull request'
release:
  header: |
    Luet is the Macaroni OS Package Manager that uses containers for building packages.
